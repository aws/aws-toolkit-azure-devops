language: node_js

node_js: 
  - "lts/*"

matrix:
  include:
    - os: windows
    - os: linux

script:
  - npm install typescript
  - npm install
  - npm run package -- --publisher travisci-build --release
  - npm test

